; fetch.no — Browser Fetch API declarations (JS backend)
; Actual mapping is in src/build/js/builtin.go
#{js}

fetch.async = (url str) (data str) {
    data = ''
}

fetch.json-async = (url str) (data str) {
    data = ''
}
