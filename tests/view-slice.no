; view-slice：区间切片是 view（零拷贝别名），不再是 clone。
; 逃逸或源被改写时自动退回 clone，保证不悬垂。

; ── 1. 切片是别名：改写原容器，视图可见 ──────────────────────
; 只有堆容器（str / []T）的切片才是 view；定长数组 [N]T 是栈上内联存储，
; 切片仍走拷贝（否则 `a[2..5]` 这种越界区间会读到相邻栈槽）。
; 数组字面量在 MIR 层是定长数组（栈上内联），其切片仍走拷贝；
; 这里用函数返回拿到真正的堆上 []i64，才能看到 view 的别名效果。
mk = () (out []i64) {
    out = [1, 2, 3, 4, 5]
}
a = mk()
b = a[1..3]
a[1] = 99
print(b[0])     ; 99 —— view 与原容器共享存储（clone 语义下是 2）
print(b[1])     ; 3

; ── 2. 逃逸到输出参数：自动 clone ────────────────────────────
get-sub = () (out []i64) {
    src = [10, 20, 30, 40]
    out = src[1..3]
}
v = get-sub()
print(v[0])     ; 20
print(v[1])     ; 30

; ── 3. 经由变量逃逸：自动 clone ──────────────────────────────
get-sub2 = () (out []i64) {
    src = [10, 20, 30, 40]
    tmp = src[1..3]
    out = tmp
}
v2 = get-sub2()
print(v2[0])    ; 20
print(v2[1])    ; 30

; ── 4. 源被重新赋值：自动 clone，视图不悬垂 ──────────────────
f = () {
    s = [1, 2, 3, 4, 5]
    w = s[1..3]
    s = [9, 9, 9]   ; 旧 buffer 被释放，w 必须已是独立副本
    print(w[0])     ; 2
    print(w[1])     ; 3
}
f()

print('PASS')
