; Test if single-char string literal works
main = () {
    sep = ','
    #{overflow = wrap}
    print('sep len: ' + sep.len-bytes().to-str())
    #{overflow = wrap}
    print('sep[0]: ' + sep[0].to-str())
}
