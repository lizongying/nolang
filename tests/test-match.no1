test-match = () {
    x ?i64

    // 保存的時候會改變，不要變成if/else 修復它
    // 直接->是else
    if x ==
err {
        log(.)
    } elif x ==
nil {
        log(.)
    } else {
        log(.)
    }

    // 全部列舉
    if x ==
err {
        log(.)
    } elif x ==
nil {
        log(.)
    } else {
        log(.)
    }

    // 這裡-> 有else的意思
    if  {
        log(.)
    }
}