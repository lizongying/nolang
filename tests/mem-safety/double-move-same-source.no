; ═════════════════════════════════════════════════════════════════
; 测试：多返回值 move 共享同一源变量
; 验证 a = x; b = x 时：
;   - a = x 走 clone（x 后续被 b = x 引用）
;   - b = x 走 move（x 后续未引用，所有权转移）
;   - 不应 double-free
; ═════════════════════════════════════════════════════════════════

get-pair = () (a []i64, b []i64) {
    x = [1, 2, 3]
    a = x   ; x 后续被引用 → clone，a 独立拥有 data
    b = x   ; x 后续未引用 → move，b 接管 x 的 data
}

main = () {
    ra, rb = get-pair()
    print(ra.len)
    print(rb.len)
    print('done')
}
