; cd src && go run ./cmd/cli/. build ../tests/i.no
; a = 1

a i16

; a = 2

; compilation error: duplicate variable 'a'
; a i16

i64 i64

i8 i8

; a.len() is the element count (live call below); the bare `.len` property form is removed
; print(a.len())

print(a.len())
; compilation error: cannot modify read-only field 'len' of string 'a'
; a.len = 2
