; test-mkdtemp-debug.no — minimal repro for fs.mkdtemp multi-assign issue

test-mkdtemp = () {
    #{overflow = wrap}
    name, ok = fs.mkdtemp('/tmp/test.XXXXXX')
    ok -> print(name) -> print('failed')
}
