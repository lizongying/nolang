; test_path_char2 — 驗證 path.no 使用 char 字面量常量後正常工作
; # std/path
print('=== path char test ===')

p = path {
    p: '/usr/local/bin'
}
print(p.p)

; path.join() 會**原地修改 .p**，同時回傳拼接後的結果。
; 早期版本無回傳值，`joined = p2.join(...)` 只會拿到空字串。
p2 = path {
    p: '/usr'
}
joined = p2.join('local/bin')
print(joined)

print('=== done ===')
