; test-tls-import-repeat — 測試 import tls 後 str.repeat 是否正常
; # std/net/tls

print('start')
space str = ' '
print('before repeat')
buf str = space.repeat(8)
print('after repeat')
buf[0] != 32 -> {
    print('FAIL: expected space')
    return
}
print('PASS: str.repeat with tls import')
