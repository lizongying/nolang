; test-hmac — 測試 hmac-sha256
; # std/crypto/hmac

print('start')
key []byte = with-cap(2)
key[0] = 1
key[1] = 2
msg []byte = with-cap(2)
msg[0] = 0xAA
msg[1] = 0xBB
print('before hmac')
a [32]byte
a = hmac.hmac-sha256(key, msg)
print('after hmac')
print(a[0])
print('done')
