; test-mkstemp-debug.no — minimal repro for fs.mkstemp multi-assign issue

test-mkstemp = () {
    name, fd = fs.mkstemp('/tmp/test.XXXXXX')
    {
        fd >= 0 -> print(name)
        -> print('failed')
    }
}
